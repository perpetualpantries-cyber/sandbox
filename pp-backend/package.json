{
  "name": "pp-backend",
  "version": "0.1.0",
  "description": "Perpetual Pantries backend — links PP (Gavin), PP Command (Ronin) and PPcanopy (Canopy)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "argon2": "^0.41.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  }
}
